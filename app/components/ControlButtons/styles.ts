// Dependencies
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    flex: 1,
  },
  container: {
    alignItems: 'center',
    marginVertical: 40,
  },
  icons: {
    flexDirection: 'row',
  },
});

export default styles;